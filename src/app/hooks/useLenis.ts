// REMOVED: Lenis smooth scrolling has been completely disabled
// Native OS scrolling is now used exclusively for premium UX

export const useLenis = () => {
  // No-op function - native scrolling only
  return null;
};

export default useLenis;